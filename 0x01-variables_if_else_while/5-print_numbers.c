#include <stdio.h>
/**
 * main - prints first ten natural numbers
 * Return:Always 0 (success)
 */
int main(void)
{
int n = '0';

while (n <= 10)
{
printf("%d", n);
n++;
}
putchar('\n');
return (0);
}
